<div class="container row">
    <h3>Comprar {{producto.nombre}}</h3>

    <div class="col-xl-6 col-sm-12 d-flex align-items-center justify-content-center">
        <img src="{{producto.ruta_img}}" alt="{{result.nombre}}">
    </div>

    <div class="col-xl-6 col-sm-12 d-flex flex-column align-items-center justify-content-center">
        <p>vale : {{{producto.precio}}}</p>
        <a href="/compra/cesta?id_producto={{producto.id}}" class="btn btn-success">Comprar</a>
    </div>
</div>